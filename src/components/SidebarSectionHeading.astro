---
import Heading from "@/components/Heading.astro";

interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
---

<Heading as="h3" class:list={["uppercase px-4 py-3 leading-none", className]}>
  {title}
</Heading>
